whenflag(){
    setsize(5)
    gotoxy(0,0)
    penclear()
    pendown()
    setpen("brightness",50)
    point(90)
    setvar($steps,0)
    repeatUntil(touching("_edge_")){
        changevar($steps,1)
        move($steps)
        turn(subtract(360,$steps))
    }
}